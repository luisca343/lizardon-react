export class MensajePersona {
  constructor (idUsuario, nombre, mensaje, fecha, imagen, transparente) {
    this.idUsuario = idUsuario
    this.nombre = nombre
    this.mensaje = mensaje
    this.fecha = fecha
    this.imagen = imagen
    this.transparente = transparente
  }
}
